### TBD
1. wikidata: if endPartyDate <= startActingDate: don't show the party (see Joachim Gauck)
2. wikidata: add photo, title of photo, and source of photo (like it is available in the csv file)
3. admin can select in csv: switch on/off Chancellors/Presidents individually
4. admin can select for wikidata which of the queries should be shown
5. add more countries like Austria and Switzerland
6. use SPARQL fabric like it is suggested in web application
7. fetch errors when reading from wikidata
8. wikidata: cache results for one day and use cache instead of calling wikidata again